console.log('running');

$(document).ready(function() {
  $('#projectsm-link').localScroll({

   target:'#projects'
  });
  $('#contact-link').localScroll({

   target:'#contact'
  });
  $('#home-link').localScroll({

   target:'#home'
  });
})
`